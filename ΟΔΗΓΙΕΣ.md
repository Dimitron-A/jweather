# 🌤️ eWeather - Οδηγίες Εγκατάστασης και Εκτέλεσης

## ⚙️ ΣΗΜΑΝΤΙΚΟ: Ρύθμιση OpenWeatherMap API Key

**ΠΡΟΣΟΧΗ! Πρέπει να πάρετε το δικό σας API key πριν τρέξετε την εφαρμογή:**

### Βήματα για το API Key:

1. **Πηγαίνετε στο:** https://openweathermap.org/
2. **Κάντε Sign Up** (δημιουργήστε δωρεάν λογαριασμό)
3. Μετά το login, πηγαίνετε στην καρτέλα **"API keys"**
4. **Αντιγράψτε** το API key που σας δίνει
5. **Ανοίξτε το αρχείο:** `eWeather/src/main/java/com/mycompany/eweather/TempUtils.java`
6. **Βρείτε τη γραμμή:**
   ```java
   private String key = "e75b6a95a3658451f77447b38cdf1a87";
   ```
7. **Αντικαταστήστε** το `e75b6a95a3658451f77447b38cdf1a87` με το δικό σας API key!

**Σημείωση:** Το API key που υπάρχει ήδη στον κώδικα ΔΕΝ λειτουργεί πια. Είναι απαραίτητο να χρησιμοποιήσετε το δικό σας!

---

## ✅ Πληροφορίες Βάσης Δεδομένων

Η βάση δεδομένων έχει ήδη δημιουργηθεί με 5 Ελληνικές πόλεις:
- **Αθήνα** (Athens)
- **Θεσσαλονίκη** (Thessaloniki)
- **Πάτρα** (Patras)
- **Ηράκλειο** (Heraklion)
- **Λάρισα** (Larissa)

### Στοιχεία Σύνδεσης:
- **URL:** `jdbc:derby://localhost:1527/weather`
- **Όνομα βάσης:** `weather`
- **Username:** `weather`
- **Password:** `weather`
- **Τοποθεσία:** Φάκελος `weather/` (στον γονικό φάκελο του eWeather)

---

## 🚀 Εκτέλεση της Εφαρμογής

Χρειάζεστε **ΔΥΟ** παράθυρα terminal:

### Παράθυρο Terminal 1: Εκκίνηση Derby Database Server

```bash
./start-derby.sh
```

**Κρατήστε αυτό το terminal ανοιχτό!** Θα δείτε:
```
Starting Derby Network Server on port 1527...
Press Ctrl+C to stop the server
```

### Παράθυρο Terminal 2: Εκτέλεση της Εφαρμογής

```bash
cd eWeather
./run-app.sh
```

Το script θα:
1. Καθαρίσει τα macOS resource fork files (διορθώνει το πρόβλημα crash)
2. Θα κάνει build το project
3. Θα ανοίξει το παράθυρο της εφαρμογής eWeather

---

## 📱 Χρήση της Εφαρμογής

### 1. Ο καιρός τώρα (Current Weather)
- Επιλέξτε μία ή περισσότερες πόλεις
- Πατήστε **"Καιρός Τώρα"** για να δείτε τον τρέχοντα καιρό
- Πατήστε **"Ανανέωση Καιρού"** για να φέρετε νέα δεδομένα από το OpenWeatherMap

### 2. Πρόβλεψη καιρού (Weather Forecast)
- Επιλέξτε μία πόλη
- Πατήστε **"Πρόβλεψη Καιρού 1ας ημέρας"** για πρόβλεψη 1 ημέρας
- Πατήστε **"Πρόβλεψη Καιρού 5 ημερών"** για πρόβλεψη 5 ημερών
- Πατήστε **"Ανανέωση Πρόβλεψης Καιρού"** για νέα δεδομένα πρόβλεψης

### 3. Στατιστικά (Statistics)
- Δείτε στατιστικά θερμοκρασίας για τις επιλεγμένες πόλεις

---

## 🛑 Τερματισμός

1. Κλείστε το παράθυρο της εφαρμογής eWeather
2. Στο Terminal 1, πατήστε `Ctrl+C` για να σταματήσετε τον Derby server

---

## ⚠️ Αντιμετώπιση Προβλημάτων

### Η εφαρμογή κλείνει αμέσως ή κάνει crash?
- Αυτό προκαλείται από τα macOS resource fork files (`._*` αρχεία)
- Το `run-app.sh` script τα καθαρίζει αυτόματα
- Βεβαιωθείτε ότι ο Derby server τρέχει στο Terminal 1

### "Connection refused" error?
- Ο Derby server δεν τρέχει
- Ξεκινήστε τον με `./start-derby.sh` στο Terminal 1

### Το API key δεν λειτουργεί?
- Βεβαιωθείτε ότι αλλάξατε το API key στο `TempUtils.java`
- Περιμένετε λίγα λεπτά μετά τη δημιουργία του key (μερικές φορές χρειάζεται χρόνος ενεργοποίησης)
- Ελέγξτε ότι το API key είναι σωστά αντιγραμμένο (χωρίς κενά)

### Ακόμα έχετε προβλήματα?
Δοκιμάστε:
```bash
cd eWeather
find . -name "._*" -delete
./run-app.sh
```

---

## 📝 Σημειώσεις

- Τα δεδομένα καιρού φέρνονται σε πραγματικό χρόνο από το OpenWeatherMap
- Τα δεδομένα αποθηκεύονται στην τοπική βάση Derby
- Μπορείτε να προσθέσετε περισσότερες πόλεις τροποποιώντας το `DatabaseSetup.java`

Καλή χρήση! 🌞
